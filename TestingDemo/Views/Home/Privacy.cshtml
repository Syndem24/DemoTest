@model TestingDemo.ViewModels.IntegrationSettingsViewModel
@{
    ViewData["Title"] = "Integration";
}

<section class="admin-account-settings admin-integration">
    <header class="admin-account-settings-hero admin-integration-hero">
        <div class="admin-integration-hero-brand">
            <img src="~/Images/Logo.png" asp-append-version="true" alt="" class="admin-integration-hero-logo" />
            <div>
                <span class="admin-account-settings-eyebrow">Admin connections</span>
                <h1>Integration</h1>
            </div>
        </div>
        <p>Store Gmail SMTP, Gemini, Groq, and Google OAuth credentials encrypted in the hotel vault. Outbound mail uses the <strong>Mori International Hotel™</strong> brand and logo. Leave a secret blank to keep the current value.</p>
    </header>

    <form asp-controller="Home" asp-action="Privacy" method="post" class="admin-integration-form">
        @Html.AntiForgeryToken()
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>

        <div class="admin-integration-grid">
            <article class="admin-settings-card admin-integration-card">
                <header class="admin-integration-card-head">
                    <div class="admin-settings-hub-icon is-teal" aria-hidden="true">
                        <svg viewBox="0 0 24 24">
                            <path d="M20 4H4c-1.1 0-1.99.9-1.99 2L2 18c0 1.1.9 2 2 2h16c1.1 0 2-.9 2-2V6c0-1.1-.9-2-2-2zm0 4-8 5-8-5V6l8 5 8-5v2z"/>
                        </svg>
                    </div>
                    <div class="admin-integration-card-copy">
                        <h2>Email delivery</h2>
                        <p>Gmail sender for staff onboarding and password reset OTP. Messages show the Mori International Hotel™ logo and trademark.</p>
                    </div>
                    @if (Model.SmtpPasswordConfigured)
                    {
                        <span class="admin-integration-status is-on">Configured</span>
                    }
                    else
                    {
                        <span class="admin-integration-status is-off">Not set</span>
                    }
                </header>

                <div class="admin-settings-fields is-single admin-integration-fields">
                    <div class="admin-settings-field">
                        <label asp-for="SenderEmail" class="form-label"></label>
                        <input asp-for="SenderEmail" class="form-control" type="email" autocomplete="off" placeholder="hotel@gmail.com" data-mori-filter="email" maxlength="254" />
                        <span asp-validation-for="SenderEmail" class="text-danger"></span>
                    </div>
                    <div class="admin-settings-field">
                        <label asp-for="SmtpPassword" class="form-label"></label>
                        <div class="admin-settings-password-group @(Model.SmtpPasswordConfigured ? "is-configured-secret" : null)">
                            @if (Model.SmtpPasswordConfigured)
                            {
                                <span class="admin-integration-secret-mask" aria-hidden="true">••••••••••••</span>
                            }
                            <input asp-for="SmtpPassword"
                                   id="SmtpPassword"
                                   class="form-control"
                                   type="password"
                                   autocomplete="new-password"
                                   placeholder="@(Model.SmtpPasswordConfigured ? "Leave blank to keep" : "Gmail app password")"
                                   aria-describedby="@(Model.SmtpPasswordConfigured ? "smtp-secret-hint" : null)" />
                        </div>
                        @if (Model.SmtpPasswordConfigured)
                        {
                            <p id="smtp-secret-hint" class="admin-integration-secret-hint">A Gmail app password is already stored. Leave blank to keep it.</p>
                        }
                        <label class="admin-settings-check">
                            <input asp-for="ClearSmtpPassword" data-clear-configured-secret="SmtpPassword" />
                            <span>Remove stored Gmail app password</span>
                        </label>
                    </div>
                </div>
            </article>

            <article class="admin-settings-card admin-integration-card">
                <header class="admin-integration-card-head">
                    <div class="admin-settings-hub-icon is-navy" aria-hidden="true">
                        <svg viewBox="0 0 24 24">
                            <path d="M20 2H4c-1.1 0-2 .9-2 2v12c0 1.1.9 2 2 2h4l4 4 4-4h4c1.1 0 2-.9 2-2V4c0-1.1-.9-2-2-2zm-2 12H6V6h12v8z"/>
                        </svg>
                    </div>
                    <div class="admin-integration-card-copy">
                        <h2>AI Integration</h2>
                        <p>Mori Assistant: simple FAQs use rules; complex multi-part questions try Gemini then Groq (when keys are stored and UseGeminiFallback is on). Dashboard shows connection status and per-provider API use.</p>
                    </div>
                    @if (Model.GeminiConfigured || Model.GroqConfigured)
                    {
                        <span class="admin-integration-status is-on">
                            @(Model.GeminiConfigured && Model.GroqConfigured
                                ? "Both set"
                                : Model.GeminiConfigured ? "Gemini set" : "Groq set")
                        </span>
                    }
                    else
                    {
                        <span class="admin-integration-status is-off">Not set</span>
                    }
                </header>

                <div class="admin-settings-fields is-single admin-integration-fields">
                    <div class="admin-settings-field">
                        <label asp-for="GeminiApiKey" class="form-label"></label>
                        <div class="admin-settings-password-group @(Model.GeminiConfigured ? "is-configured-secret" : null)">
                            @if (Model.GeminiConfigured)
                            {
                                <span class="admin-integration-secret-mask" aria-hidden="true">••••••••••••</span>
                            }
                            <input asp-for="GeminiApiKey"
                                   id="GeminiApiKey"
                                   class="form-control"
                                   type="password"
                                   autocomplete="new-password"
                                   spellcheck="false"
                                   placeholder="@(Model.GeminiConfigured ? "Leave blank to keep current key" : "Paste API key from Google AI Studio")"
                                   aria-describedby="gemini-api-hint@(Model.GeminiConfigured ? " gemini-secret-hint" : null)" />
                        </div>
                        <p id="gemini-api-hint" class="admin-integration-secret-hint">Primary AI. Stored encrypted; never shown again after save.</p>
                        @if (Model.GeminiConfigured)
                        {
                            <p id="gemini-secret-hint" class="admin-integration-secret-hint">A Gemini API key is already stored. Leave blank to keep it.</p>
                        }
                        <label class="admin-settings-check">
                            <input asp-for="ClearGeminiKey" data-clear-configured-secret="GeminiApiKey" />
                            <span>Remove stored Gemini API key</span>
                        </label>
                    </div>

                    <div class="admin-settings-field">
                        <label asp-for="GeminiKeyName" class="form-label">Gemini key nickname</label>
                        <input asp-for="GeminiKeyName"
                               class="form-control"
                               autocomplete="off"
                               maxlength="80"
                               placeholder="e.g. Mori production Gemini" />
                    </div>

                    <div class="admin-settings-field">
                        <label asp-for="GroqApiKey" class="form-label"></label>
                        <div class="admin-settings-password-group @(Model.GroqConfigured ? "is-configured-secret" : null)">
                            @if (Model.GroqConfigured)
                            {
                                <span class="admin-integration-secret-mask" aria-hidden="true">••••••••••••</span>
                            }
                            <input asp-for="GroqApiKey"
                                   id="GroqApiKey"
                                   class="form-control"
                                   type="password"
                                   autocomplete="new-password"
                                   spellcheck="false"
                                   placeholder="@(Model.GroqConfigured ? "Leave blank to keep current key" : "Paste API key from Groq Console")"
                                   aria-describedby="groq-api-hint@(Model.GroqConfigured ? " groq-secret-hint" : null)" />
                        </div>
                        <p id="groq-api-hint" class="admin-integration-secret-hint">Backup AI when Gemini is missing or out of quota. Stored encrypted.</p>
                        @if (Model.GroqConfigured)
                        {
                            <p id="groq-secret-hint" class="admin-integration-secret-hint">A Groq API key is already stored. Leave blank to keep it.</p>
                        }
                        <label class="admin-settings-check">
                            <input asp-for="ClearGroqKey" data-clear-configured-secret="GroqApiKey" />
                            <span>Remove stored Groq API key</span>
                        </label>
                    </div>

                    <div class="admin-settings-field">
                        <label asp-for="GroqKeyName" class="form-label">Groq key nickname</label>
                        <input asp-for="GroqKeyName"
                               class="form-control"
                               autocomplete="off"
                               maxlength="80"
                               placeholder="e.g. Mori backup Groq" />
                    </div>
                </div>
            </article>

            <article class="admin-settings-card admin-integration-card">
                <header class="admin-integration-card-head">
                    <div class="admin-settings-hub-icon is-teal" aria-hidden="true">
                        <svg viewBox="0 0 24 24">
                            <path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm0 3c1.66 0 3 1.34 3 3s-1.34 3-3 3-3-1.34-3-3 1.34-3 3-3zm0 14.2c-2.5 0-4.71-1.28-6-3.22.03-1.99 4-3.08 6-3.28 2.01.2 5.97 1.29 6 3.28-1.29 1.94-3.5 3.22-6 3.22z"/>
                        </svg>
                    </div>
                    <div class="admin-integration-card-copy">
                        <h2>Google sign-in</h2>
                        <p>OAuth Client ID and Client Secret for guest/staff Google login.</p>
                    </div>
                    @if (Model.GoogleClientSecretConfigured && !string.IsNullOrWhiteSpace(Model.GoogleClientId))
                    {
                        <span class="admin-integration-status is-on">Configured</span>
                    }
                    else
                    {
                        <span class="admin-integration-status is-off">Not set</span>
                    }
                </header>

                <div class="admin-settings-fields is-single admin-integration-fields">
                    <div class="admin-settings-field">
                        <label class="admin-settings-check">
                            <input asp-for="GoogleLoginEnabled" />
                            <span>Show “Continue with Google” on the login page</span>
                        </label>
                        <p class="admin-integration-secret-hint">When off, the Google button is hidden even if credentials are saved.</p>
                    </div>
                    <div class="admin-settings-field">
                        <label asp-for="GoogleClientId" class="form-label"></label>
                        <input asp-for="GoogleClientId" class="form-control" autocomplete="off" placeholder="….apps.googleusercontent.com" />
                        <span asp-validation-for="GoogleClientId" class="text-danger"></span>
                    </div>
                    <div class="admin-settings-field">
                        <label asp-for="GoogleClientSecret" class="form-label"></label>
                        <div class="admin-settings-password-group @(Model.GoogleClientSecretConfigured ? "is-configured-secret" : null)">
                            @if (Model.GoogleClientSecretConfigured)
                            {
                                <span class="admin-integration-secret-mask" aria-hidden="true">••••••••••••</span>
                            }
                            <input asp-for="GoogleClientSecret"
                                   id="GoogleClientSecret"
                                   class="form-control"
                                   type="password"
                                   autocomplete="new-password"
                                   placeholder="@(Model.GoogleClientSecretConfigured ? "Leave blank to keep" : "Google Client Secret")"
                                   aria-describedby="@(Model.GoogleClientSecretConfigured ? "google-secret-hint" : null)" />
                        </div>
                        @if (Model.GoogleClientSecretConfigured)
                        {
                            <p id="google-secret-hint" class="admin-integration-secret-hint">A Google Client Secret is already stored. Leave blank to keep it.</p>
                        }
                        <label class="admin-settings-check">
                            <input asp-for="ClearGoogleClientSecret" data-clear-configured-secret="GoogleClientSecret" />
                            <span>Remove stored Google Client Secret</span>
                        </label>
                    </div>
                </div>
            </article>
        </div>

        <article class="admin-settings-card admin-integration-confirm">
            <header class="admin-integration-card-head">
                <div class="admin-settings-hub-icon is-navy" aria-hidden="true">
                    <svg viewBox="0 0 24 24">
                        <path d="M18 8h-1V6c0-2.76-2.24-5-5-5S7 3.24 7 6v2H6c-1.1 0-2 .9-2 2v10c0 1.1.9 2 2 2h12c1.1 0 2-.9 2-2V10c0-1.1-.9-2-2-2zm-6 9c-1.1 0-2-.9-2-2s.9-2 2-2 2 .9 2 2-.9 2-2 2zm3.1-9H8.9V6c0-1.71 1.39-3.1 3.1-3.1 1.71 0 3.1 1.39 3.1 3.1v2z"/>
                    </svg>
                </div>
                <div class="admin-integration-card-copy">
                    <h2>Confirm it is you</h2>
                    <p>Enter your current AdminManager password to save secrets or send a test email.</p>
                </div>
            </header>

            <div class="admin-settings-fields is-single admin-integration-fields">
                <div class="admin-settings-field">
                    <label asp-for="CurrentPassword" class="form-label"></label>
                    <div class="admin-settings-password-group">
                        <input asp-for="CurrentPassword"
                               id="CurrentPassword"
                               class="form-control"
                               type="password"
                               autocomplete="current-password"
                               placeholder="Current password" />
                    </div>
                    <span asp-validation-for="CurrentPassword" class="text-danger"></span>
                </div>
            </div>

            <div class="admin-settings-actions admin-integration-actions">
                <button type="submit" class="btn btn-primary">Save secrets</button>
                <button type="submit" class="btn btn-outline-secondary" formaction="@Url.Action("TestSmtp", "Home")">Send test email</button>
            </div>
        </article>
    </form>
</section>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <script>
        function syncConfiguredSecret(group) {
            if (!group || !group.classList.contains('is-configured-secret')) return;
            var input = group.querySelector('input.form-control');
            var clearing = group.parentElement && group.parentElement.querySelector('[data-clear-configured-secret]:checked');
            group.classList.toggle('is-editing', Boolean(clearing || (input && input.value)));
        }

        document.querySelectorAll('.admin-settings-password-group.is-configured-secret').forEach(function (group) {
            var input = group.querySelector('input.form-control');
            if (!input) return;
            ['input', 'focus', 'blur'].forEach(function (eventName) {
                input.addEventListener(eventName, function () { syncConfiguredSecret(group); });
            });
            syncConfiguredSecret(group);
        });

        document.querySelectorAll('[data-clear-configured-secret]').forEach(function (clearBox) {
            clearBox.addEventListener('change', function () {
                var input = document.getElementById(clearBox.getAttribute('data-clear-configured-secret'));
                var group = input && input.closest('.admin-settings-password-group');
                syncConfiguredSecret(group);
            });
        });
    </script>
}
