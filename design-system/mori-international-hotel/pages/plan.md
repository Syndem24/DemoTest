# Plan — retired

> `/plan` is no longer a guest page. Old `/plan` URLs redirect to `/#room-types` on the landing page.

Room types are browsed on the home gallery. **View accommodations** scrolls to **Our room types**. **View room** opens the shared `_RoomTypeDetailSheet` modal and stays on Home.
